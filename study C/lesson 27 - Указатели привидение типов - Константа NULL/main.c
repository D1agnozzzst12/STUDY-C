#include <stdio.h>

int main() {

    // int arg = 7;
    // int *ptr_arg, *ptr;

    // ptr_arg = &arg;
    // ptr = ptr_arg;

    // printf("ptr = %d, arg = %d", *ptr, arg);

    // *ptr_arg = 100;

    // printf("ptr = %d, arg = %d", *ptr, arg);





    // int arg = 777;
    // int *ptr_arg;
    // char *ptr;

    // ptr_arg = &arg;
    // ptr = (char *)ptr_arg;
    // *ptr = 1;
    // printf("*ptr_arg = %d, arg = %d\n", *ptr_arg, arg);



    // int *ptr;
    // *ptr = 1;

    int arg = 5;
    int *ptr = NULL;
    *ptr = 1;

        if(ptr != NULL) {
            *ptr = 1;
        }

    return 0;
}