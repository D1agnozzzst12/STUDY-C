#include <stdio.h>

// double per_sq(double w, double h)
// {
//     if(w < 0 || h < 0) {
//         return 0;
//     }

//     return 2 * (w, h);
// }






// double per_sq(double, double);
#include "func.h"
int main()
{
    printf("per = %.2f\n", per_sq(2.5, 3.5));
    abs_int(-5);
    sq_to_int(-5.5);
    return 0;
}
